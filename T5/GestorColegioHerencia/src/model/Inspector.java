package model;

public interface Inspector {
    // se definen solo métodos abs y public, por lo que no hace falta poner lo que se da por hecho
    // siempre pubplic abs ... -> POR DEFECTO, al ser interfaz

    void realizarInspeccion();
}
